<%= render 'user_error', user: @user %>
<%= render 'user_info', user: @user %>
<%= render 'user_edit_link', user: @user %>
<%= render 'books/new_book', book: @book %>

</div>

<div class="col-md-8 offset-md-1">
  <h2 class="mt-3">Users</h2>
  <table class="table">
    <thead>
      <tr>
        <th>image</th>
        <th>名前</th>
      </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
      <tr>
        <td class="col-4"><%= image_tag user.get_profile_image(80,80) %></td>
        <td class="col-5"><%= user.name %></td>
        <td class="col-2"><%= link_to "Show", user_path(user.id) %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>